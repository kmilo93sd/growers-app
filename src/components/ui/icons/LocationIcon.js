import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';

const LocationIcon = (props) => (
  <Icon name="location-sharp" color={'black'} {...props} />
);

export default LocationIcon;
