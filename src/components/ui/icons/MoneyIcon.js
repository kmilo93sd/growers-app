import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';

const MoneyIcon = (props) => (
  <Icon name="cash-outline" color={'green'} {...props} />
);

export default MoneyIcon;
